diff --git a/Docker.rust b/Docker.rust
index 2405f300d52..4e6b439ea01 100644
--- a/Docker.rust
+++ b/Docker.rust
@@ -4,7 +4,7 @@ FROM devkitpro/devkita64:latest
 RUN apt-get update && \
     apt-get install -y \
         python3-pip python3-setuptools python3-wheel \
-        ninja-build pkg-config flex bison curl \
+        ninja-build pkg-config flex bison curl glslang-tools \
         libclang-dev clang cmake git \
         llvm-15-dev libllvmspirvlib-15-dev libclc-15-dev libclang-15-dev \
     && apt-get clean
@@ -32,6 +32,7 @@ RUN cd /tmp && \
 RUN curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain nightly && \
     . $HOME/.cargo/env && \
     rustup component add rust-src && \
+    rustup target add aarch64-unknown-linux-gnu && \
     cargo install bindgen-cli cbindgen
 
 ENV PATH="/root/.cargo/bin:${PATH}"
diff --git a/build-switch.sh b/build-switch.sh
index cc2722da8cb..435e03f2782 100755
--- a/build-switch.sh
+++ b/build-switch.sh
@@ -1,8 +1,14 @@
 #!/bin/bash
 set -e
 
-IMAGE_NAME="devkitpro-mesa-rust"
+IMAGE_NAME="${MESA_SWITCH_IMAGE_NAME:-devkitpro-mesa-rust}"
 CONTAINER_NAME="mesa-switch-build"
+export MESA_SWITCH_RUST_TARGET="${MESA_SWITCH_RUST_TARGET:-aarch64-unknown-linux-gnu}"
+
+DOCKER_SECURITY_ARGS=()
+if command -v getenforce >/dev/null 2>&1 && [ "$(getenforce)" = "Enforcing" ]; then
+    DOCKER_SECURITY_ARGS+=(--security-opt label=disable)
+fi
 
 # Build the Docker image if needed.
 if ! docker image inspect "$IMAGE_NAME" >/dev/null 2>&1; then
@@ -13,6 +19,8 @@ fi
 # Start the build container.
 docker rm -f "$CONTAINER_NAME" 2>/dev/null || true
 docker run -d --name "$CONTAINER_NAME" \
+    "${DOCKER_SECURITY_ARGS[@]}" \
+    -e MESA_SWITCH_RUST_TARGET="$MESA_SWITCH_RUST_TARGET" \
     -v "$(pwd):/project" --workdir "/project" \
     "$IMAGE_NAME" sleep infinity
 
@@ -95,7 +103,7 @@ run 'ninja -C /project/builddir-native src/compiler/clc/mesa_clc src/compiler/sp
 # Configure the cross build.
 echo "=== Configuring cross build (Switch + nouveau + nouveau_vk) ==="
 run '
-export PATH="/usr/local/libexec:/project/builddir-native/src/compiler/clc:/project/builddir-native/src/compiler/spirv:$PATH"
+export PATH="/project/builddir-native/src/compiler/clc:/project/builddir-native/src/compiler/spirv:$PATH:/usr/local/libexec"
 cd /project && meson setup builddir-switch --wipe \
     --cross-file switch_cross_file.txt \
     --buildtype=release \
@@ -123,7 +131,7 @@ cd /project && meson setup builddir-switch --wipe \
 # Build Switch archives.
 echo "=== Building Mesa for Switch ==="
 run '
-export PATH="/usr/local/libexec:/project/builddir-native/src/compiler/clc:/project/builddir-native/src/compiler/spirv:$PATH"
+export PATH="/project/builddir-native/src/compiler/clc:/project/builddir-native/src/compiler/spirv:$PATH:/usr/local/libexec"
 ninja -C /project/builddir-switch \
     src/nouveau/vulkan/libnvk.a \
     src/nouveau/vulkan/libvulkan.a \
